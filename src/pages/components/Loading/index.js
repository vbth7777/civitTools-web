function Loading() {
  return <div style={{
    cursor: 'default'
  }}>
    <h1
      className={cx('title')}
      style={{
        textAlign: 'center',
        padding: '20px',
      }}>Loading...</h1>
  </div>
}
export default Loading;
